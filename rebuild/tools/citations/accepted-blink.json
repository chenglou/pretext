[
{"kind":"gap-name","token":"han-kerning","where":"code","count":2,"reason":"X1: the three sites that halted a character with a trim measured from Canvas (the start context, the end context, the line-end reshape of an open or close mark) raise through one function, gaps.ts hanKerningTrim; the line-end halt Canvas can't show keeps its own, hanKerningEndUnknown"},
{"kind":"gap-name","token":"glyph-clusters","where":"code","count":1,"reason":"X1: a position asked inside a grapheme raised the same gap from the hang width's walk and from the glyph clusters of an item; both call gaps.ts positionInsideGrapheme"},
{"kind":"gap-name","token":"unsafe-to-break","where":"code","count":1,"reason":"X1: a glyph cluster's two edges raised the pair placement gap from two lines of shapeOf; both call gaps.ts pairPlacement, which also holds the test (limits.ts pairPlacementUnknown)"},
{"kind":"prose","token":"control item without a control kind","where":"code","count":1,"reason":"X3: a control item's kind is one of the union's members (types.ts InlineItem), so no item is a control without a kind and the line breaker's switch has no case to throw from"},
{"kind":"generated","token":"e46e717f33c1ebd4c8f154e2e91a85061d1cb4378356ccaca22077952c584b4b","where":"code","count":1,"reason":"X3: tools/gen-blink-data.ts writes HarfBuzz's OpenType language tags as [code, tag, ...] records sorted by code instead of one 'code=TAG|...' string the port parsed into a Map; the same 1,000-odd languages and tags from the same hb-ot-tag-table.hh, and every other export of generated/break-tables.ts is byte for byte the same (the diff is the constant and its comment)"},
{"kind":"source","token":"line_breaker.cc:186-188","where":"code","count":1,"reason":"X3: the line breaker's space test (space or tab) was written twice, in line-breaker.ts and gaps.ts; both import content.ts isSpaceLB, which keeps the citation"}
]
