- orchestrator, 08:40: rebuild/lab/score.ts widths and painter metrics for safari/webkit-host now pass on exact float32 edges (LTR: fround(left + fround(width)) === right; RTL: fround(right - fround(width)) === left) instead of 1/64 snapping, because WebKit inline positions are float32 px (892 of 1,314 Safari smoke widths are off the 1/64 grid). Histograms still count 1/64 units. Blink (1/(64*dpr)) and Gecko (1/60) keep grid equality.
- gecko owner, 09:05: rebuild/src/paint.ts hyphenSpan, gecko case, adds `white-space: nowrap` to the hyphen span. Under `overflow-wrap: break-word` an overflowing painted line offered a word-wrap break before the separate hyphen frame and wrapped (9 of 297 Firefox smoke cases, e.g. c-5bb28a79f6310f0d); in the paragraph the hyphen belongs to its frame. Only the gecko branch changes.
- orchestrator, 09:10: rebuild/lab/run.ts serves case chunks as ASCII-only JSON (all chars above U+007E escaped), because JSON parsed from a body with any raw non-Latin-1 character gives 16-bit strings even for ASCII values, which switches WebKit's (and Blink's) 16-bit-only rules for every ASCII case in the chunk (specs/probes-safari.md cross-check). Rows recorded before this change may carry that effect for WebKit: rerun WebKit case sets that mix CJK/Arabic with ASCII before trusting keep-all or first-unit emergency-break results. Also from the cross-check: WebKit's process-wide TextBreakingPositionCache keys content by value with pre-wrap and break-spaces sharing a context, so identical text can reuse breaks computed under another style; that is a page-history effect, reported through --order runs + --native-compare.
- blink owner, 09:15: rebuild/src/model.ts GapName gains 'han-kerning' (additive). Blink's HanKerning trims fullwidth punctuation with `halt` using characters outside the shaped range and at line ends (han_kerning.cc AppendFontFeatures, shaping_line_breaker.cc:344-378); Canvas shows none of that context, and no existing gap name covered it (DESIGN-REVIEW §3.1 noted the missing name).
- blink owner, 09:24: rebuild/src/measure/canvas.ts gains measureTextBounds (additive): measureText plus actualBoundingBoxLeft/Right, logged as a call. Blink's HanKerning::FontData types fullwidth dots, colons and quotes from glyph bounds against half the advance (han_kerning.cc CharTypeFromBounds), and measureText's width alone can't give that.
- orchestrator, 10:30: lab maintainer (lab-issues-2) changed score.ts: controls other than TAB/LF/CR with a positive rect now count as visible; line starts compare at native cluster starts (graphemes split at text node edges or Thai break-all). Notes for owners are under each entry in rebuild/lab/ISSUES.md: Blink — Chrome gives 1,185 of 1,429 control characters an advance where the port predicts none (310 width, 70 painter failures); Gecko — Firefox gives U+0000/VT/FF a 1px advance (2+2 failures); WebKit — 386 ❤️ cases where the port starts a line at U+FE0F. Rows now carry env.documentCaseIndex and env.previousCaseId for page-history analysis.
- blink owner, 10:50: rebuild/src/model.ts GapName gains 'tab-stops' (additive). Blink counts tab stops from SimpleFontData::SpaceWidth, the platform space advance without `trak` tracking and before 16.16 truncation (simple_font_data.cc:225-240, font.cc:303-340); Canvas gives the tracked 16.16 advance. Probe blink-followups F4 shows the difference in 16px Helvetica Neue (stops at 35.5859375px, not 8 × 4.453125); the audit (specs/blink-AUDIT.md B6) asked for a name instead of silence.
- gecko owner, 11:25: rebuild/tools/ppucd.ts gains forEachPpucdBlock (additive): visits `block` lines with resolved props. forEachPpucdRange visits only `cp` and `unassigned` lines, and ppucd writes no cp line for 210,383 code points whose values equal their block's: CJK Ext A (U+3400..4DBF except U+3405), Ext B, D, F-J, the private use areas and surrogates. The Gecko props equality test against icu_properties 2.1.2 (engines/gecko/props.test.ts) found General_Category Cn for U+3400; gen-gecko-data.ts now fills block values first. Owners whose generators read ppucd through forEachPpucdRange alone may have the same hole for those ranges.
- painter owner, 12:20: rebuild/src/paint.ts drops the gecko owner's `white-space: nowrap` on the Gecko hyphen span (09:05). Every painted line that ends with a hyphen fragment, or starts with the U+200D of R7, now gets `text-wrap-mode: nowrap` on the line block, in all three engines: an overflowing painted line broke before the hyphen span (Chrome and webkit-host at the chosen soft hyphen, 1,066 and 1,009 painter-failing cases) or after the leading joiner (Firefox), where the paragraph offered no break. specs/PAINTER-RESULTS.md has the runs. No engine output changed.
- painter owner, 12:20, note for the blink owner (no change made): engines/blink/index.ts lineOutput gives `cr-ff` control items (CR and FF in preserve modes, content.ts appendPreserveWhitespace) the kind `collapsed`, because only text items and tabs count as text. Blink keeps them in text_content as control items, so the painter never paints them and merges the text on both sides into one item: `extra` CR `ordinary` in `pre` paints 237.125px against the native and predicted 237.1328125px (ws/controls `c-35d44d2989d1a6e4`, 11 painter failures). Painting them needs a `text` fragment (width 0) instead of `collapsed`, which isn't an additive change.
- gecko owner, 22:36 (rebuild-charter): rebuild/lab/tsconfig.json includes ./observe/*.ts (additive), so `bunx tsc -p rebuild/lab/tsconfig.json` checks the observation ports and their tests. Also add-only sparse checkouts in ~/github/browser-engines/firefox-156.0: intl/icu/source/data/misc and intl/icu/source/common (Firefox's bundled ICU 78.3; tools/gen-gecko-data.ts reads langInfo.txt, uscript.h and uloc.cpp for the likely-subtags module).
- tests owner, 23:40 (rebuild-charter): rebuild/lab/run.ts's readBuild and userAgentMatches moved unchanged into rebuild/lab/browser-build.ts, which run.ts imports, so the probe runner reads the build the same way. rebuild/probes/runner.ts records `build` in its output and rebuild/probes/types.ts gains `ProbeOutput.build?` (additive; outputs before this carry none), so facts extracted from a probe run name their build (rebuild/tests/facts.ts).
- tests owner, 23:40 (rebuild-charter): new directories rebuild/tests (rule registry, rule-targeted families, derivation, facts, coverage, layered gate, independence test; `bunx tsc -p rebuild/tests/tsconfig.json`, `bun test rebuild/tests`) and rebuild/facts (versioned probe facts per engine build). No library file changed.
- font facts, 23:55 (rebuild-charter): rebuild/lab/predictor.ts attaches font facts: withFacts(font) now takes the engine and `c.fontFixtures` and returns `fontFactsFor(font, engine, fixtures)` from the new rebuild/lab/font-facts.ts, instead of UNKNOWN_FONT_FACTS; nothing else in predictor.ts changed and case ids don't. The data is rebuild/lab/font-facts.json, generated offline on macOS 27.0 (26A428) by .artifacts/charter-20260916/font-facts/tools (Core Text probe, fontTools 4.65.0 table reader, builder), with per-face columns, file hashes and the engine rule behind each fact; tests in rebuild/lab/font-facts.test.ts. rebuild/lab/tsconfig.json gains `compilerOptions.resolveJsonModule` (additive) for the JSON import. Rows from before this carry null facts: expect joining-technology, optical-size, hyphen-glyph and fixed-pitch-path gaps to disappear for known fonts, and Blink lines at Arabic shaping-call edges to change. DESIGN.md §1.2's "Until then lab/predictor.ts passes UNKNOWN_FONT_FACTS" is now out of date.
- architect, 2026-09-17 (stage 5, DESIGN.md §8.3): not additive. rebuild/src/model.ts replaces the flat `runs` with a tree of inline content (`ParagraphOf.content`: text leaves, spans with their computed wrapping styles, box edges and `vertical-align`, atomic inlines, `<br>`, `<wbr>`), adds the block's `textIndent` and `textAlign`, `LineSlot`/`FULL_WIDTH`, `LineResultOf` (`line` or `below-floats`), `BelowFloats` on `ParagraphLayout`, `slot`/`indented`/`align` on every line, the fragment kinds `box-start`, `box-end`, `atomic`, `br`, `wbr`, new per-engine geometry items and line fields, and `ExpectedObservation.elements`. `TextRunOf` is gone. New rebuild/src/content.ts (`indexContent`, `styleUnder`, `langUnder`) with content.test.ts. rebuild/src/engines/engine.ts: `nextLine(prepared, start, slot, measurer)` returns `LineResultOf`; `UnportedFeature`. rebuild/src/index.ts: `prepareParagraph`, `firstLineStart`, `layoutLine`, `paragraphGaps`, `layoutParagraph(paragraph, env, slots?)`. rebuild/src/env.ts: `BlinkProcessLanguages`, `WebKitProcessLanguages`, `GeckoProcessLanguages`, `ProcessLanguages` composed into the environments and `GivenFacts` (field names unchanged; sources cited). rebuild/src/paint.ts paints the tree. rebuild/lab/types.ts keeps the flat case types as the lab's own and re-exports `InlineParagraph`, `InlineNode`, `InlineElement`, `LineSlot`; rebuild/lab/predictor.ts turns flat cases into trees, so case ids and baselines don't move. Engines, observation ports and page.ts's `recordedLayout` don't compile until their owners do stage 5.
- webkit owner, 11:13: rebuild/src/model.ts GapName gains 'dictionary-breaks-stand-in' (additive). The running browser's word segmenter stands in for the line iterator's dictionary engines (DESIGN §6.3); in WebKit it differs from libicucore's line boundaries exactly where an engine range starts with a combining mark (27 of 282,337 SA positions in runtime-parity/sa), which no existing name covered (webkit-AUDIT B4).
- feature families, 2026-09-17 (stage 5, DESIGN.md §8.3, the lab owner's case-format and page items as far as the families need them): not additive for case files that carry structure, additive for flat ones. rebuild/lab/types.ts: `Case.inline?: InlineStructure` (`content`, `textIndent`, `textAlign`, `lineSlots`), `NativeObservation.elements?` (Element.getClientRects() per element) and `floats?` (slot float boxes). rebuild/lab/cases/case.ts: `leafRuns`, `flatStructure`; a case with structure hashes `{ fontFixtures, inline, pageLang, paragraph }` under `pretext-lab-case/2`, and its `paragraph.runs` must list the tree's leaves; `makeCase` drops a flat structure, so flat cases and every existing id stay as they were. rebuild/lab/cases/build.ts: `treeParagraph`, `leaf`, `el`, `atomic`, `br`, `wbr`. rebuild/lab/page.ts builds the tree natively (nested spans with logical box edges, `vertical-align` and the block's line height; atomics as empty top-aligned inline-blocks; `<br>`, `<wbr>`; text-indent, text-align; slot floats before the content) and records `elements` and `floats`; flat cases build the same DOM as before. rebuild/lab/run.ts accepts structured cases and `--chrome-apple-languages` with `--chrome-accept-languages` (rebuild/lab/languages.ts `ChromeLanguages`, defaults unchanged). rebuild/lab/predictor.ts passes a case's tree with font facts and `lineSlots` to `layoutParagraph`. score.ts is unchanged: it doesn't compare `elements` or apply the slot-rows assumption yet. rebuild/tests: `derive.ts` derives structured cases (pass A without slots or text-indent, sized passes no narrower than a row's insets, the first row's insets and the indent added to derived thresholds, rounds under other process languages refused); registry kind `observer assumption`; 55 provisional rules in rule-changes.json.
- webkit owner, 2026-09-17 (stage 5): rebuild/src/model.ts `WebKitTextBox` gains `expansionBehavior: { left, right }` (additive; only the WebKit engine and its observation port build these boxes). `text-align: justify` expands runs through `InlineContentAligner`, which gives each run an expansion behavior (left forbidden after an expansion, right forbidden on the line's last content run, InlineContentAligner.cpp:150-228), and the complex text controller recounts the box's opportunities with that behavior and places the expansion among its glyphs (ComplexTextController.cpp:107-118, :673-696, :800-845). Without it the observation port can't place code point edges inside a justified box.
- webkit owner, 2026-09-17 (stage 5): rebuild/src/model.ts `WebKitTextBox` gains `shapedAcrossBoxes: boolean` (additive; built only by the WebKit engine and its observation port). Safari 27 shapes complex RTL text across undecorated inline box edges as one run (`TextShapingAcrossInlineBoxes`, on by default; InlineLineBuilder.cpp:780-1028), and each box's width is its characters' share of that shaping, which Canvas gives only at the range's ends. The observation port marks such boxes' rect values limited.
- blink owner, 2026-09-17 (stage 5): rebuild/DESIGN.md §2.7 Blink bullet gains `afterLeadingFloats` (additive). The Blink port's break token (engines/blink/types.ts `BlinkLineStart`) carries whether the leading floats were placed. Without it a first formatted line whose text-indent alone overflows a narrowed slot rewinds the indent into an empty line (line_breaker.cc:4225-4248), and the shared loop lays the same start out in the same slot forever: feature family case `c-0262c91b5a593353` stalled the lab page. No shared code changed.
- webkit owner, 2026-09-17 (stage 5): rebuild/src/model.ts `LineResultOf`'s `below-floats` gains `next?: Start` (additive), and rebuild/src/index.ts `fillLines` lays the next slot out from `result.next` when given, else the same start as before. DESIGN.md §2.9 gains the matching sentence in the shared loop and the WebKit row. The lab protocol places the slot floats before the content, so WebKit's first line build places them itself (tryPlacingFloatBox, InlineLineBuilder.cpp:1394-1396) and its tab stops count from the indent alone (m_lineContentEdgeOffset, :478), while a later build, including the retry after a refused slot, finds them in the formatting context. Without a next start the retry can't tell (feature families `rule/line-slots`: 309 width, break and line count failures with tabs beside a left float). Blink and Gecko return no `next`, so their loops don't change.
- gecko owner, 2026-09-17 (stage 5): new probe rebuild/probes/gecko-font-matching.ts (additive; Firefox only, measurement only), gecko-port F4: Canvas strings that reproduce the DOM's advances for a unit starting with a cluster extender or U+202F after an invalid character, and text-presentation © in "Apple Color Emoji". Output in .artifacts/probes/gecko/font-matching; the Gecko port's rangeAu and emoji font test cite it. No shared library file changed.
- gecko owner, 2026-09-17 (stage 5): new probe rebuild/probes/gecko-slot-indent.ts (additive; Firefox only, measurement only), gecko-port F5. With text-indent wider than the first row's band, Firefox places row 0's right float at y 32px, below the first line (floats in the first line's content must fit beside its indent), so the lab protocol's rows don't hold (DESIGN.md §2.9). Output in .artifacts/probes/gecko/slot-indent. Note for the lab and families owners: rule/line-slots cases whose text-indent exceeds a row's band need another float arrangement, or the slot-rows assumption must mark them unobserved. No library file changed.
- lab owner, 2026-09-17 (ceiling round 2): rebuild/src/model.ts `Gap` gains `at?: { start: number; end: number }` (additive): the source range a condition concerns, UTF-16 offsets into the concatenated text leaves, or a break offset with start === end. Scorer 4 (rebuild/lab/score.ts) attributes a failing line only to gaps that concern it: the failing line's own `gaps`, the previous line box's `gaps` (the break decision the failing line starts from) and those of lines without a line box between, the `belowFloats` refusals between the two, and paragraph gaps whose `at` meets those lines' source range. A paragraph gap without `at` covers no line; it is listed as `paragraphGaps` in the per-case `lineGaps`. Owners whose conditions concern characters or offsets rather than the whole paragraph should give `at`, or move the gap onto the line whose breaks decide it. No engine output changes until an owner sets it.
- lab owner, 2026-09-17 (ceiling round 2): rebuild/lab/languages.ts and run.ts give Safari and webkit-host their `preferredLanguages` before launch, from `webkit-host --print-languages` (rebuild/tools/webkit-host/main.swift, rebuilt), which evaluates WebKit's steps from the UI process's AppleLanguages to the WebContent process's list; the page only checks the first entry. On this Mac the given list is zh-CN,zh-Hans where round 1 took zh-CN from the page, so webkit-host rows from now on carry another environment key: `score.ts --native-rows` refuses to combine them with round 1 rows, and gates seeded from round 1 refuse them (exit 2). The library's Han rule takes the first zh- entry, zh-CN either way. Rows record the helper's steps in `languages.webContent`.
- painter owner, 2026-09-17 (round 2): rebuild/src/paint.ts `PaintableLayout` becomes a union by engine with `belowFloats` (additive for callers: `lab/predictor.ts` passes the `ParagraphLayout`, which carries both), and the Blink branch reads `geometry.needsAccurateEndPosition`. New painted forms, no engine output changed: a line from a later line build than the paragraph's first gets its slot floats from a holder block around the line block; a line that ended at a soft wrap ends with an empty inline-block wider than any band where the engine's trailing white-space rules read whether content follows (WebKit and Gecko: lines ending in hanging white space; Blink: a trimmed space where the line's end isn't reshaped); Blink paints hanging spaces as their own text node; `<wbr>` fragments are painted as `<wbr>` elements. DESIGN.md §7 has the sources.
- gecko owner, 2026-09-17 (stage 5): new probe rebuild/probes/gecko-rtl-rects.ts (additive; Firefox only, measurement only), gecko-port F6. "Hello world again" in 13px Georgia: in a 300px or 4000px block, RTL or LTR, each code point's Range rect x relative to the first equals the frame's integer app unit prefix within 0.01 au; in a 100000px block, RTL and right-aligned LTR alike, the relative x values drift by up to 0.69 au (1459.688 for 1459, 2068.125 for 2067), since rect values that far from the origin come back in float32 steps of 1/128px (0.47 au). Output in .artifacts/probes/gecko/rtl-rects. Note for the lab and scorer owners: the rule families' RTL paragraphs sit in a 100000px block, so their code point x facts can differ by 1 au from exact predictions (111 Firefox rows in round 13, also in rows without inline objects); a 1 au tolerance for x values beyond 2^16 px, or a narrower block, would remove the noise. No library file changed.
- gecko owner, 2026-09-17 (ceiling round 2): rebuild/src/model.ts `GeckoCharacter` gains `unitStart: boolean` (required; only the Gecko engine and its observation port build these): whether the unit begins a shaping unit (gfxFont::SplitAndInitTextRun, gfxFont.cpp:3708-3900). lab/observe/gecko.ts reads it instead of its own `\p{M}` stand-in for IsClusterExtender (CHARTER-CRITIC item 13), and marks a point limited under `in-word-prefix` only where an end of its advance sum is inside a shaping unit. Frame boxes, the x of later frames on the line and element rects are engine output and now predicted: the port no longer names a gap where the layout reports none (ROUND1-CRITIC item 5). Facts tallies move values from limited to predicted; no metric changes.
- gecko owner, 2026-09-17 (ceiling round 2): the Gecko engine's paragraph gaps carry `at` (the lab owner's `Gap.at`): ui-language and page-history the leaf, font-size-quantization, optical-size and letter-spacing glyph-clusters the text run's source range, space-in-shaping the measured stretch, the emoji font-fallback and bitmap-emoji-size the cluster, U+2007/U+2008 font-fallback the character, dictionary-breaks-unavailable each complex-script stretch. `in-word-prefix` line gaps carry the offset. A gap reported once per paragraph before is now reported per range.
- gecko owner, 2026-09-17 (ceiling round 2): new probes rebuild/probes/gecko-round2.ts (F7 unit widths against OffscreenCanvas and canvas elements, F8 digits in an 8-bit run under lang="ko", F9 a partial `fi` ligature) and rebuild/probes/gecko-round2b.ts (F10 family coverage through LastResort, F11 emoji box metrics, F12 how kerning divides between a pair's glyphs), additive, Firefox only, measurement only. Outputs in .artifacts/probes/gecko/round2 and round2b. For the lab and fact owners: F12 shows HarfBuzz's legacy `kern` machine giving each glyph of a pair half the adjustment in Times New Roman, Verdana, Helvetica, Helvetica Neue (and Geeza Pro's Latin fallback), and GPOS giving it to the first glyph in Arial, Hiragino Sans and Apple SD Gothic Neo; Canvas totals can't tell them apart, so a font fact would be needed to predict in-word advances there. No shared library file changed.
- gecko owner, 2026-09-17 (ceiling round 2): rebuild/src/model.ts `GeckoFrameGeometry` gains `{ kind: 'wbr'; element; level; x; width }` (additive; built only by the Gecko engine). The Gecko layout places a `<wbr>`'s WBRFrame, 0 × 0, and lab/observe/gecko.ts reports it as the element's one rect, as Firefox does (round 1 feature rows: `c-00370d538345f01b` reports x 3558 au, width 0, height 0 after a 3558 au frame). Consumers that switch over Gecko frame kinds need the case; nothing outside the Gecko engine and port does today.
- blink owner, 2026-09-17 (round 2): rebuild/src/model.ts `FontFacts` gains `pairKerning: 'first-advance' | 'split' | null` and `UNKNOWN_FONT_FACTS` gains `pairKerning: null` (additive; only Blink reads it). Where HarfBuzz puts a pair adjustment between two glyphs isn't in Canvas totals: GPOS PairPos XAdvance on the first glyph, or the kern and kerx pair machine's kern >> 1 on the first glyph and the rest on the second (hb-kern.hh:102-106), chosen by the font's GPOS, kern and kerx tables (hb-ot-shape.cc:150-185 at harfbuzz dfdc088c, Chrome 153's pin). Times New Roman, Helvetica Neue and Hoefler Text kern through the legacy table, so Blink's positions at kerned line edges differ from the first-glyph default by d − (d >> 1) (features `rule/text-align`, families `rule/following-space`). Research tooling: .artifacts/charter-20260916/font-facts/tools/tables.py gains a `kerning` column (GPOS kern scripts and PairPos value formats, kern and kerx subtable formats), build-facts.ts a `pairKerning` face column and env overrides for its inputs and output; rebuild/lab/font-facts.json is regenerated from them (every other column and top-level key byte-equal to before; 44 faces 'first-advance', 32 'split', 108 null) and rebuild/lab/font-facts.ts gives the fact from the primary faces. rebuild/lab/observe/blink.test.ts's facts literal gains the field.
- blink owner, 2026-09-17 (round 2): rebuild/lab/observe/blink.ts marks an in-item edge limited only under a gap the layout reports concerning it (a gap of the rect's line, or a paragraph gap whose `at` range meets the range), else predicted; the port no longer names a gap of its own. It indexes the ranged paragraph gaps by 64-unit source blocks and returns the first meeting gap in the layout's order, as a scan would: a held-out suite paragraph with 5,317 ranges stalled the page when every code point scanned them all. rebuild/src/engines/blink computes HanKerning::MayApply from a per-paragraph count (`BlinkPrepared.hanKerningCandidates`) instead of scanning the group at every position; layouts and Canvas call logs are unchanged (specs/blink-RESULTS.md "A stalled held-out suite run").
- blink owner, 2026-09-17 (ceiling round 3): rebuild/src/model.ts `BlinkGlyphCluster` gains `startLimit?: GapName` and Blink's `text` item `sizeLimit?: GapName` (additive, optional; built only by the Blink engine and read by lab/observe/blink.ts). They mark the positions inside an item that are Canvas stand-ins: between letters HarfBuzz joins (`in-word-prefix`), where a font's lookups may form one glyph cluster and the declaration gives no ligature fact (`glyph-clusters`), and where no fact places a pair adjustment (`unsafe-to-break`). The observation port reports a rect edge as predicted only where the layout knows both the item's x and the position inside it; a gap on a character now limits every position that sums its advance and, through the item's size, the x of what follows on the line (round 2 reported 2,030 development cases' in-word positions as exact that weren't). No metric reads the states.
- blink owner, 2026-09-17 (ceiling round 3): rebuild/src/env.ts gains `SOURCE_IDENTICAL_BUILDS` and rebuild/src/index.ts `buildGaps` reads it (additive): Chrome 153.0.8010.50 reports no `engine-build` gap, since `git diff --name-only 153.0.8010.48 153.0.8010.50` in the Chromium checkout lists chrome/VERSION alone and DEPS is unchanged. WebKit's and Gecko's lists are empty.
- lab owner, 2026-09-17 (ceiling round 3): every lab and probe job now launches pinned byte-identical copies of Chrome 153.0.8010.50 and Firefox 156.0 under ~/github/browser-engines/apps (rebuild/lab/browser-build.ts `LAB_APPS`, `pin-browser.sh`; Chrome with `--disable-updater-scheduler`, Firefox profiles with `app.update.auto` false); rows keep `build` unchanged, and run records gain `app`, `bundleSha256`, `parts` and `measurements`. rebuild/lab/run.ts and page.ts gain parts (`--part-cases`, `--part-ms`, `--parts-from`; a `retire` reply ends a page; installed Safari defaults to 5-minute parts in fresh tabs) and `--record-measurements` (rebuild/lab/record.ts, measurements.ts); rows and the scorer's inputs don't change. New tools: rebuild/lab/sharded.ts (a case file as parallel jobs, `--isolate` for one fresh process a case) and compare-rows.ts. rebuild/tests: import-rules.ts takes reclassified entries for any rule and moves hand edits of rules.json into rule-changes.json (change rules there only; rules.json gains `generated` hashes), coverage.ts finds tests named `describe > test`, facts.ts joins a fact by the probe label before a colon, observe-families.sh observes through sharded.ts without pauses, rerun-probes.sh reruns every probe set of the previous facts file. Families: `joining` gains a `shy-mark` word and `text-align` an ideograph word, and four families name round 2's six rules, so the family case files changed (TESTS.md §6, "Round 3 derivations"). rebuild/facts/blink/153.0.8010.50.ndjson is new.
- painter owner, 2026-09-17 (ceiling round 3): rebuild/src/paint.ts `PaintableLayout` reads more of each engine's line (additive for callers, `lab/predictor.ts` passes the `ParagraphLayout`): Blink `geometry.width`, `hangWidth`, `availableWidth`; WebKit `geometry.contentWidth`, `hangingWidth`, `lineBoxWidth`, `boxes` and `next.offset`, `next.previousLine.carriedWidth`; Gecko `geometry.width`, `hang`, `availableWidth`; and the fonts' `facts.pairKerning`. It imports `unicode/bidi.ts`, `unicode/ubidi.ts` and `unicode/grapheme.ts`, and from the Blink port `engines/blink/script.ts` (`scriptsPerUnit`) and `props.ts` (`USCRIPT_LATIN`), which a bundle with the painter now carries. New export `painterLimits(paragraph, layout)`: per line with a line box, the named limits of what painting the line alone can't reproduce (`PainterLimitName`, DESIGN.md §7 "Limits"); it isn't exported from `src/index.ts` and the lab doesn't record it yet. New painted forms, no engine output changed (DESIGN.md §7 has the sources; specs/PAINTER-RESULTS.md "Round 3" the runs): override spans hold whole elements, and elements and text holders keep the paragraph's direction; the soft wrap box also follows a last character that a bidi paragraph's end would reset, inside its override span, and Blink's hanging spaces; Blink's hanging spaces take one of three forms; forced breaks and `<br>` are painted, and collapsed white space between two pieces of a leaf; a line that reaches past its band doesn't wrap; WebKit text nodes get the leaf's string storage through `deleteData`; Gecko gets a space or newline after a last tab or formatting character under letter spacing; Blink starts a line with U+061C where the port's script runs say the line alone loses the Arabic run it continued and the mark gives it back without changing the line's bidi levels. New file rebuild/src/paint.test.ts. Note for the Blink owner: whether the text before a line's trailing space was reshaped is in the port's shape views and not in `BlinkLineGeometry`; the painter derives it from widths and styles (`hangingForm`) and misses the whole-part reshape (`first_safe.offset >= break_opportunity.offset`), 24 pairs.
- blink owner, 2026-09-17 (ceiling round 3): new probe rebuild/probes/blink-round3.ts (additive; Chrome only, measurement only): R1, a word-final letter before a space in Noto Nastaliq Urdu, DOM against Canvas through U+2028 (the adjustment reads the letter before it too, which a window of one cluster on each side misses); R2, whether Canvas shapes a font word by word (`ب` U+3000 `ب` under 0 and 1/64 px of letter spacing). Output in .artifacts/probes/blink/round3. rebuild/tools/gen-blink-data.ts and the generated blink-break-tables.ts gain Default_Ignorable_Code_Point, Emoji_Component, Emoji, Emoji_Presentation, Emoji_Modifier_Base, General_Category Lm, Sk and Cn bits and HarfBuzz's language system tags per ISO 639 code (hb-ot-tag-table.hh at dfdc088c); only the Blink port reads them. The Blink engine now reads `FontFacts.fonts` (coverage, ligatures, scriptLookups), so model.ts's "No engine reads them yet" is out of date for Blink.
- blink owner, 2026-09-17 (ceiling round 3), note for the lab owner (no change made): Chrome 153.0.8010.50 never returns from `Range.getClientRects()` over one code point of fresh case `c-1fda71ce84fd9989` (what the case is: withheld, see platform-bugs entry 13; probe outputs .artifacts/probes/blink/round3-native-hang-*), so the lab page stalls at its native step whatever the predictor. The case is set aside in .artifacts/lab/fresh/chrome/r3-blink-2/parts/excluded-native-hang.ndjson. A fresh round that draws such a case needs a way to skip it. Note for the scorer owner: `suite/U+FFFC/start` (3 rows, c-23e11e5c3a96497d) is counted uncovered because the failing line is taken to be the one after the line whose U+FFFC differs (a soft hyphen's copied rect puts the next letter on both native lines), where the `font-fallback` range doesn't reach; and a point gap on a neighbouring line never covers (score.ts:794), so a break-decision gap has to be a range.
- gecko owner, 2026-09-17 to 09-18 (ceiling round 3; recorded after the owner was stopped, from its diff): shared files it changed. rebuild/src/measure/canvas.ts: `CanvasSettings.element?: boolean` (additive), the context of a detached `document.createElement('canvas')` instead of an OffscreenCanvas, part of the context key; only the Gecko port asks for it. The file's key separator is a NUL byte, so edit it byte-safe. rebuild/src/env.ts: `GeckoEnvironment.canvasElement?: boolean`, set by `detectEnvironment` where `document.createElement` exists; absent in the unit tests' stubs, where the port keeps the OffscreenCanvas path. rebuild/src/model.ts (required fields, built only by the Gecko engine and read by lab/observe/gecko.ts): `GeckoCharacter.standInBefore`, `GeckoTextFrame.standInAtEnd` and `GeckoTextFrame.advancesStandIn: GapName | null`; the port also reports the limited state `float32-precision` for edges 2^16 device px or more from the origin (nsLayoutUtils.cpp:2517-2537), which DESIGN.md §5 lists for Blink only. DESIGN.md §4.1, the §4.2 `font` cell and the §4.3 Gecko note describe the canvas element; §5's gap table still describes round 2's Gecko conditions. New probe file rebuild/probes/gecko-round3.ts (F13 to F19, Firefox only, measurement only, raw values without checks; outputs in .artifacts/probes/gecko/round3 and round3-f15 to round3-f19). Notes for other owners: measuring a string with U+FE0E pins text fonts for the document's later text in Firefox (held-out c-6403c221b98778d6), so nothing should add U+FE0E to a measured string; the Gecko engine reads `FontFacts.fonts` coverage, ligatures and scriptLookups, and `joining` for marked ligature groups; painter-only failures without a covered explanation rose on Firefox sets because more widths pass (202 Noto Naskh Arabic rows whose painted joined line edge differs by 33 to 64 au); lab/residual-classes.json still registers Gecko's 1 au class as inferred, and it has no members on the canvas element; round 3's Gecko rules aren't in rebuild/tests/rules.json.
- font checks owner, 2026-09-18 (round 4a): new rebuild/src/measure/font-checks.ts and its test. rebuild/src/index.ts `prepareParagraph` now lays out `withLearnedFontFacts(paragraph, env, measurer)`, the caller's paragraph with the font facts Canvas answered, and `PreparedParagraph.paragraph` is that paragraph; a supplied fact is never checked, and Gecko is asked nothing. rebuild/src/model.ts `FontFacts` comments say which facts are asked of Canvas. No engine file changed. The checks' contexts carry `partition: 'font-checks'` and their calls are in each layout's call log, so offline replay per case works; if a measurer later outlives a paragraph, the first paragraph of a declaration carries them and replay per case needs the store's answers. New probe rebuild/probes/font-checks.ts (the library's own module bundled into the page; verdicts by `.artifacts/lab/font-checks/tools/verdict.ts`). Notes for other owners: predict-only scoring wasn't valid for Firefox while the canvas element was in use (137 emoji cases differed from full runs); with `pairKerning` unknown Blink reports positions between kerned glyphs inside a line as predicted with no gap (43 rows behind the table under no facts, e.g. `c-0f3ea3e4202d62d0`); `ListedFontFacts.coverage` is a whole set, so a per-code-point coverage check has nowhere to put its answer.
- tests owner, 2026-09-18 (round 4a): rebuild/src/index.ts exports `painterLimits` and the types `PainterLimit`, `PainterLimitName` (one line; lab/predictor-core.ts `limits()` uses it). No other library file changed. In the lab: `predictor-core.ts` is the one prediction adapter, and `predictor.ts` and `baselines/no-facts-predictor.ts` are made from it; `rows.ts` reads rows plain or `.zst` for every tool; scorer 6 (`SCORER_VERSION` 6: report-only rects, WebKit box width at a moved x, sum-only lines, painter limits recorded per painted line, gap firing per case); `lab/residual-classes.json` is gone, `fresh.ts` reads the scorer's per-case `residual`; `rebuild/tests/gate.ts seed` needs `--staging`; `rebuild/bench/run.ts` launches the pinned Chrome and Firefox. New: rebuild/tests/{sets,replay,browser-sets,ledger}.ts with their tests, rebuild/tests/reference/ (six pinned manifests) and rebuild/tests/baselines/staged-round4-sets/. The references, ledgers and staged seeds describe the round 3 library until they are recorded again.
- gecko owner, 2026-09-18 (round 4a): shared files changed by removal only, for the decision that Firefox measures on an OffscreenCanvas always. rebuild/src/measure/canvas.ts is round 2's file again: `CanvasSettings.element` is gone (its key separator is a NUL byte: restore it with git, never retype it in an editor). rebuild/src/env.ts loses `GeckoEnvironment.canvasElement` and its line in `detectEnvironment`. Nothing else read either. Gecko-only: `GeckoPrepared.tabSpacingPrefix` (null without a tab), `Provider.tabStandIn`, and the Gecko layout can report `float32-precision` on a shaping unit 2^18 px or wider. lab/observe/gecko.ts reads `layout.gaps` and limits values that sum text a ranged paragraph gap names, except `dictionary-breaks-unavailable`, and its `float32-precision` bound follows the line's app units per device pixel. New probe rebuild/probes/gecko-round4.ts (F20 to F27); gecko-round2, round2b and round3 return `checks`; 83 facts added to rebuild/facts/gecko/156.0.ndjson. `likely.test.ts` has an explicit timeout (it passed bun's 5 s default under load). Left for the tests owner: the residual registry entries (`gecko/synthetic-bold-offset`, the 1 au class's new probed strings), the four Gecko follow-up sets in rebuild/tests/rerun-probes.sh, `c-f3e8314c35b33990`'s attribution, and the history-dependent emoji set that isn't stable between identical runs.
- blink owner, 2026-09-18 (round 4a): rebuild/src/model.ts, Blink section only. `BlinkGlyphCluster`: the doc says `textStart`/`textEnd` are the characters the caret code counts and `graphemeStarts` the starts Blink lists; new optional `graphemesLimit?: GapName`. New type `BlinkShapeRun = { textStart, textEnd, reshaped: { textStart, textEnd } | null, fontsKnown }`. The Blink text item gains `runs: BlinkShapeRun[]` and `partsKnown: boolean`, both required; only the Blink engine builds text items, and rebuild/lab/observe/blink.test.ts fixtures are updated. `paint.ts` doesn't read them yet: `runs[].reshaped` is the field the painter asked for, and a wrapped line whose first cluster kept an adjustment with the previous line's last cluster (`c-0ee8c36920378f9f`) needs a painter limit.
- webkit owner, 2026-09-18 (round 4a): no shared code edited. New engine data and its tools: rebuild/data/webkit/coretext-macos27/css-families.tsv (macOS 27.0's per-language CSS families, 1,079 languages) dumped by rebuild/data/webkit/tools/ct-css-families.m, one line added to rebuild/data/webkit/FILES.tsv, generators rebuild/tools/gen-webkit-fonts.ts and gen-webkit-joining.ts (→ engines/webkit/generated/fonts.ts and joining.ts), probe rebuild/probes/webkit-round4.ts. Notes for the font facts owner: PingFang coverage is read from the downloaded asset, which the WebContent process doesn't see (it gets the reserved PingFangUI.ttc: no kana, no U+2027); STHeiti, Kaiti SC, LiHei Pro, LiSong Pro, BIZ UDGothic and Osaka don't resolve in the process; `generics.webkit` lists the settings' families only, where the DOM uses Core Text's per-language ones (Menlo for monospace under en).
- bugs dossier, 2026-09-18 (round 4a): new rebuild/platform-bugs/ (LEDGER.md, pages/, results/, verify.ts). Nothing filed.
- shared owner, 2026-09-18 (round 4b): rebuild/src/env.ts `detectEngine()` now also asks the running Canvas for what the engine's measuring recipes assume and answers `unsupported` by name where something is missing (new rebuild/src/measure/canvas-checks.ts with its test; two contexts, two `measureText` calls, no measurer and no call log). `detectEnvironment(given)` keeps its behaviour and asks Canvas nothing: it reads the engine from the user agent through the same patterns (`engineFromUserAgent`, not exported). The lab's predictor never calls `detectEngine()`, so recordings, tier 1 and tier 2 are unchanged; for the tests owner: call it once per page load outside any case's recorded phases (or the probe below per release) and fail the run on `unsupported`, so a lab run in a browser like Firefox 140 ESR refuses. New probe rebuild/probes/canvas-checks.ts. rebuild/platform-bugs/verify.ts records a page that hangs (`STEP …` titles, `hangs` in pages/index.json), for entry 13; entries 13 and 14 with pages and results. New rebuild/knip.config.ts (`bunx knip --config rebuild/knip.config.ts`). The rule registry gained round 4's rules through rule-changes.json (535 current). CHARTER.md's known deviations are now "Standing" and "Removed"; REPORT.md gained "Round 4a" and lost the charter evaluation's closing notes (in git history at 584e359).
- port fixes, 2026-09-18 (round 4c): no shared code edited. rebuild/tests/sets.ts gains the development set `rich-prewrap` (1,334 cases, `.artifacts/lab/cases/rich-prewrap.ndjson` from `bun rebuild/lab/cases/rich-prewrap.ts --out=…`, sha256 61bdf919…): the other tier sets don't reach three of the rules research/PREWRAP-RICH.md found (tab-size on a span, justify beside a preserved newline or beside preserved spaces across a box end), so this round's fixes changed 0 of their predictions in Chrome and 2 in Firefox. The references, ledgers and seeds to be recorded after round 4 take it in with the rest; both orders with recording cost 4 to 6 s a browser. rebuild/tests/rule-changes.json: 4 rules added (`gecko/lines/preserved-newline-ends-line-in-br`, `blink/output/end-offset-for-justify`, and round 4b's `blink/output/box-fragment-edges` and `webkit/output/first-and-last-display-box`, which had annotations or tests and no registry row) and `gecko/measure/tab-width-containing-block` restated (539 current). rebuild/tests/known-tail.json: 4 items for the new set (45). rebuild/TESTS.md §3 and rebuild/lab/README.md "Test tiers" say so.
- evaluator, 2026-09-18 (round 4 evaluation): no library or observation port file edited; every run of the evaluation holds one bundle per configuration. Lab tools only: rebuild/lab/fresh.ts takes the `rich-prewrap` kind (`cases/rich-prewrap.ts` under the seed, a flat kind other browsers copy) and `--config=no-facts|facts`, which picks the tiers' predictor and names the runs, report and round record after the configuration, so one seed's cases and parts serve both; rebuild/lab/cases/used-ids.ts `inThisRepository` follows a named case file that is a dangling symbolic link into a removed worktree (the charter evaluation's derived family links broke when ~/github/pretext-rebuild-charter was removed, and `seal.ts` refused to run), with a test in cases/parts.test.ts. New data: rebuild/lab/baselines/sealed-4-20260918.json (the sealed-4 record without its seed), staged seeds with attributed seed records in rebuild/lab/baselines/staged-round4-{no-facts,facts}/, rebuild/tests/baselines/staged-round4-{no-facts,facts}/ and rebuild/tests/baselines/staged-round4c-sets/ (nothing adopted), and 14 items and the evaluation's named cases in rebuild/tests/known-tail.json (59 items). Every one of main's suite cases has been used: fresh sets and sealed-4 hold none.
- shared owner, 2026-09-18 (re-architecture step 1, S1 of research/ARCHITECTURE-PLAN-2.md: the lab owns the row): no engine logic edited, and the library's bundle differs only by what left it. Out of rebuild/src/model.ts: `ParagraphLayout` with `BlinkLayout`, `WebKitLayout` and `GeckoLayout`, `BelowFloats`, and the row's `LineOf` with `BlinkLine`, `WebKitLine` and `GeckoLine`, now in rebuild/lab/types.ts with a `MeasureLog` of the lab's own; and the observation contract (`Expected`, `ExpectedRect`, `UnobservableFact`, `ExpectedObservation`, `CanvasMeasure`, `ObservationPort`, with a `CanvasSettings` of the lab's own), now in the new rebuild/lab/observe/contract.ts. Out of rebuild/src/index.ts: `layoutParagraph` and `fillLines`, now in rebuild/lab/predictor-core.ts over the engines' `firstLine` and `nextLine`; `prepareParagraph`, `firstLineStart`, `layoutLine` and `paragraphGaps` stay. What an engine returns until its X1 stays in the library: `LineOf` and `LineResultOf` moved from model.ts to rebuild/src/engines/engine.ts, beside the interface that returns them, and each engine's `types.ts` names its own (`BlinkLine`, `BlinkLineResult`, and so on), so model.ts no longer holds them; `LineResult` is in index.ts. rebuild/src/paint.ts spells `PaintedLine` and the refused rows it reads, types only. rebuild/src/engines/gecko/gecko.test.ts has its own slot loop over `prepareParagraph`, as the Blink and WebKit tests had. rebuild/tests/independence.test.ts: model.ts names an engine 31 times, from 70. rebuild/tools/citations/accepted-shared.json accepts by name the 12 tokens that went to the lab with the contract's comments and the loop. rebuild/bench/page.ts imports the lab's `layoutParagraph` and compiles no further than before.
- shared owner, 2026-09-18 (re-architecture step 1, S2 of research/ARCHITECTURE-PLAN-2.md: shared code stops selecting engines): no ported rule edited; every prediction, question and painted line of the six references is the same. Outside comments, no file of rebuild/src names an engine but index.ts, env.ts, the engines' own and paint.ts (rebuild/tests/independence.test.ts lists paint.ts alone, 74 mentions, until the painter's split). New per engine: `geometry.ts` (its line geometry from model.ts and its line start from its `types.ts`: types only, the one engine file the lab imports), `data.ts` (its break rules, pair table, grapheme rules and `BidiData`, parsed when the module loads; WebKit's joins its existing data.ts) and `checks.ts` (what it asks of the runtime checks: `CanvasNeeds`, and `FontChecks`: the facts it reads, the layout zoom, whether its context takes `lang`, its contexts' text rendering). `git mv`: breaks/generated/{blink-break-tables,webkit-break-tables}.ts to engines/{blink,webkit}/generated/break-tables.ts (same sha256) and breaks/generated/gecko-break-data.ts to engines/gecko/generated/break-data.ts (its import line alone changes), with the generators' output paths; breaks/tables.ts to breaks/pair-table.ts, which keeps `pairCanBreak` alone. Gone: `bidiDataFor`, `graphemeRulesFor` (unicode/bidi.ts now exports `unicode17BidiClasses` and `libicucoreBidiClasses`), the six table loaders, rebuild/src/engines/engine.ts (`EngineImplementation`; `UnportedFeature`, which nothing threw; `LineOf` and `LineResultOf` are in model.ts, `LineStart` in index.ts), and rbbi's rule status, which nothing read. Signatures: `missingCanvasSupport(needs)`, `withLearnedFontFacts(paragraph, checks, measurer)`; `prepareParagraph` and `detectEngine()` hand each engine's data over. rebuild/src/index.ts exports the same names but `UnportedFeature`. rebuild/probes/font-checks.ts bundles each port's checks.ts beside the font checks. rebuild/bench/page.ts spells the engine objects' shape itself and compiles no further than before. rebuild/tools/citations/accepted-shared.json accepts 7 losses by name. Tier 1 in Chrome exits 3 by the string storage rule alone, as it has since rebuild/src/measure/canvas-checks.ts changed after the references' commit: this step adds measure/font-checks.ts, measure/canvas-checks.ts and three lines of engines/blink/shape.ts (two import lines, and `blinkGraphemeRules` for `graphemeRulesFor('blink')`) to the files it names.
- shared owner, 2026-09-18 (re-architecture step 1, S3 of research/ARCHITECTURE-PLAN-2.md: the width in the slot, and the function set): no ported rule edited; every prediction, Canvas question and painted line of the six references is the same, and tier 2 forward shows 0 transitions in the three browsers and both configurations. rebuild/src/model.ts: `LineSlot` is `{ width, left, right }`, `Paragraph.width` and `FULL_WIDTH` are gone, and `FillResultOf`, `LinePieces<Facts>` and `LineInspectionOf` are what the function set returns; `LineOf` and `LineResultOf` stay while the ports' `nextLine` returns them. Every port reads the width from the slot at its one or two sites (blink/line-breaker.ts and blink/index.ts, webkit/lines.ts twice, gecko/lines.ts), so one prepared paragraph serves any width. Each engine's index.ts exports `prepare(paragraph, env, inspect)`, `firstLine`, `fillLine`, `linePieces`, `inspectLine` and `paragraphGaps` over its `nextLine`, which still computes pieces, geometry and gaps for every line, plain or inspected; `inspectLine` and `paragraphGaps` throw on a plain paragraph; a decided line carries an `engine` tag, as a line start does, so rebuild/src/index.ts can dispatch on it. A filled line's source range is on the fill result (`start`, `end`), which the plan's set didn't place anywhere: a row and a line-ranges prediction need it, and it is where the line breaks, known without pieces. Each port's prepared paragraph holds its `Measurer` and `inspect`; no function takes a measurer, and rebuild/src/index.ts is the dispatch (`Prepared = { engine, state }`). rebuild/src/measure/canvas.ts gains `Context`, `contextFor`, `width` and `bounds` beside the index API, over one list of contexts found by comparing settings (the joined key with its NUL separator is gone); the measured string still reaches Canvas as the object the port built, on the same contexts, in the same order. rebuild/src/measure/font-checks.ts measures through them and keeps what it keeps in lists local to one call: its contexts, the declarations it resolved, and the questions it asked with their answers, since the checks share questions the memo used to answer (without that list 4,692 Chrome and 27,014 webkit-host no-facts cases repeat a question); the WeakMap store and the JSON keys are gone, and its calls are no longer in the library's log. So the lab counts Canvas work itself (rebuild/lab/predictor-core.ts, on the page's Canvas classes, arguments untouched; the counts equal the log's on every smoke row of the three browsers; `memoHits` is 0), rebuild/lab/record.ts stops joining the library's log (`declared`, `library`), and rebuild/tests/replay.ts `pack` drops `logDisagrees`. The adapter makes the frozen row from the function set (per line `fillLine`, `inspectLine`, `linePieces`; a line keeps its slot's two insets), the observation ports take the width (`observe/contract.ts`), `LayoutPrediction` carries it, and rebuild/src/paint.ts reads the painted block's width from the line's slot, which the adapter puts back before it paints (painter differential: 389,646 of 389,646 byte-equal). New: rebuild/lab/baselines/plain-predictor.ts and other-widths-first-predictor.ts, `compare-rows.ts` and `compare-sets.ts --prediction=without-measure`, rebuild/src/test-lines.ts (test support: the ports' tests walk the function set). rebuild/bench/page.ts is rewritten over the function set with count, pieces and inspect modes and compiles; bench/run.ts finds its lock among the wrapper's slots. rebuild/probes/blink-storage.ts S5 and rebuild/probes/font-checks.ts run the function set and the call-local checks (S5 called `layoutParagraph`, which left rebuild/src/index.ts in S1). `replay.ts pack` resolves a run's parts under `--runs` and reads them before it empties its folder. `bun rebuild/tests/function-set.ts plain|pure|sweep` exits 0 on the six references.
- blink owner, 2026-09-18 (re-architecture step 2, X1): no ported rule edited; tier 1 the same on all six references (389,646 of 389,646, 0 changed questions; exit 3 in Chrome by the string storage rule alone, because `engines/blink/shape.ts` differs from the references' commit); tier 2 in Chrome, both orders and both configurations, 0 transitions. A Blink paragraph is prepared plain or inspected (`BlinkPrepared.inspect`); `engines/blink/gaps.ts` holds every gap condition behind a sink that is null on a plain paragraph; `fillLine` keeps `LineBreaker::NextLine`'s `LineInfo` as the decided line and gives the source range from the two line starts; `pieces.ts`, `inspect.ts` and `limits.ts` (the limits, moved out of `shape.ts`) read it and write nothing, and justification's sizes go from `justificationOf` to `itemsOf` as data; S3's wrapper (`nextLine`, `lineOutput`, `BlinkLine`, `BlinkLineResult`) and the prepared fields `gaps`, `collapsedAt` and `sourceRuns` are gone; the Blink port no longer uses `LineOf` or `LineResultOf`. `LineBreaker.nextLine()` keeps Blink's name. An inspected paragraph still makes the hyphen's one-byte contexts, used or not (3,745 recorded cases of each configuration count them); a plain one doesn't. New `rebuild/tools/citations/accepted-blink.json` (three gap-name counts accepted by name). For the registry: the statement of `blink/measure/spaces-stay-in-neutral-latin-range` now says what `spacesStay` tests, "a space, a character other than white space, no soft hyphen and no character with a script of its own" (rebuild/tests/rule-changes.json, rules.json generated from it). No shared file changed.
- webkit owner, 2026-09-18 (re-architecture step 2, X1): no ported rule edited; tier 1 exits 0 in both webkit-host configurations, every prediction and question the same; the function set against the start commit's differs in 0 of 63,987 cases in either configuration (fill results, pieces with `overflows` and the paint facts, inspections, paragraph gaps, the inspected path's question order); tier 2 forward 0 transitions in webkit-host, both configurations. A WebKit paragraph is prepared plain or inspected (`WebKitPrepared.inspect`: the paragraph's gaps, the box facts only gaps read, the history worlds); `engines/webkit/gaps.ts` holds every gap; `fillLine` (`lines.ts`) returns the decided line without building fragments or display boxes; `linePieces` and `lineGeometry` (the new `output.ts`) and `lineGaps` read it and write nothing; `webkitNextLine` and S3's wrapper are gone, with the fill state's `reverted`, `decisionStart` and `overflowStart`, which nothing read; the WebKit port no longer uses `LineOf` or `LineResultOf`. Imports now form cycles between functions (lines.ts, gaps.ts and content.ts; gaps.ts, output.ts and lines.ts); no module reads another's values when it loads. Not run: tier 2 in both orders, the sweep after the change, installed Safari. No shared file changed.
- gecko owner, 2026-09-18 (re-architecture step 2, X1): no ported rule edited; tier 1 the same on all six references; tier 2 forward 0 transitions in Firefox, both configurations. A Gecko paragraph is prepared plain or inspected (`GeckoPrepared.inspect`); `engines/gecko/gaps.ts` holds every gap; `fillLine` is the passes alone and leaves a decided line; `placement.ts`, `pieces.ts` and `inspect.ts` read it and write nothing; `measure.ts` and `advance.ts` hold moved text; `nextGeckoLine` and `lineOutput` are gone; the Gecko port no longer uses `LineOf` or `LineResultOf` outside its test. New `rebuild/tools/citations/accepted-gecko.json` (one accepted loss). No shared file changed.
- orchestrator, 2026-09-18 (re-architecture step 2, X1's check 1): the three owners each reported that `bun rebuild/tests/function-set.ts plain` exits 1 by its order rule alone, with equal fill results and pieces, no question the lab's path didn't ask and no more contexts on every case. The lab's path asks inspection's questions between two fills, so a later fill's repeat of one is a memo hit there and a first ask on the plain path: no path that asks less can keep the lab path's order. rebuild/tests/function-set.ts `plain` now fails on results that differ, on a question the lab's path didn't ask, on more contexts and on `inspectLine` answering, and counts the passing cases whose first asks come in another order (at the merge Chrome 26,035 without facts and 21,826 with, Firefox 11,418 and 11,422, webkit-host 1,174 and 1,218). rebuild/tests/replay.ts `classifyAsked` also returns `added`, `reordered` and `dropped`; tier 1 classifies as before. rebuild/tests/function-set.test.ts: the planted other order passes and is counted. The plain predictor's browser run (`browser-sets.ts --predictor=rebuild/lab/baselines/plain-predictor.ts`, `compare-sets.ts --prediction=line-ranges`) covers question order and belongs to every milestone that changes the plain path's questions. Chrome's two replay references moved to the X1 merge, every prediction and question the same. Documents: rebuild/DESIGN.md §2.1, §2.7, §2.8, §2.9, §4.5, §5 and §8.1 describe the three ports' decided lines, `gaps.ts` and the plain path's counts; rebuild/TESTS.md and rebuild/lab/README.md "Checks for the re-architecture" describe the check and the browser evidence; rebuild/bench/README.md says its three modes no longer do the same work; research/ARCHITECTURE-PLAN-2.md has a dated note at the order rule.
- painter owner, 2026-09-19 (re-architecture step 3 of research/ARCHITECTURE-PLAN-2.md: the painter): no painted form and no limit changed. Tier 1 the same on all six references, the painter differential byte-equal on 389,646 cases (a planted flip of `overflows` in the adapter is caught, exit 1), tier 2 forward 0 transitions in the three browsers and both configurations. rebuild/src/paint.ts names no engine: `paintLines(paragraph, lines, refusedRows, rules, document)` and `painterLimits(paragraph, lines, rules)` take `PaintLine<Facts>` per line (`linePieces`' result, the slot with its width, `hasLineBox`) and the engine's `PaintRules<Facts>`, new in rebuild/src/engines/<engine>/paint-rules.ts (`blinkPaintRules`, `webkitPaintRules`, `geckoPaintRules`, exported from `src/index.ts`). `PaintableLayout` and `PaintedLine` are gone. `overflows` and the engines' paint facts have their first reader. rebuild/lab/types.ts `LayoutPrediction` gains `painter` (what `paint()` and `limits()` call; no part of a row), made in rebuild/lab/predictor-core.ts, whose `paintable()` is gone. rebuild/tests/independence.test.ts: the list of shared files that name an engine is empty. Found and not fixed: the root cause of the two open painter rows of `twins` (rebuild/tests/known-tail.json, `blink/painter-brackets-shaped-otherwise-than-the-paragraph`). For everyone who writes files with a tool: a unicode escape (backslash, u and four hex digits) in new file content came out as the literal character, a NUL byte among them; check new files with `grep -a`.
- blink owner, 2026-09-19 (re-architecture step 2, X2): no ported rule edited; tier 1 repeats only on Chrome's references (65,900 cases without facts, 65,898 with; exit 3 also by the string storage rule, because `engines/blink/shape.ts` changed) and the same on the other four; tier 2 in Chrome, both orders and both configurations, 0 transitions, and every row equals X1's. The Blink port holds its Canvas contexts (`StyleContexts` per style, the list in `BlinkPrepared.canvases`) and no longer imports the index API, the memo or the log; `BlinkPrepared.measurer` and `Shaper.m` are gone. rebuild/probes/blink-storage.ts S5 notes Canvas's answers on the page instead of reading the library's log and finds a context's partition through `prepared.state.canvases`; rebuild/tools/twin-scan.ts's anchor and tap follow `measure16`'s new measuring line and name a context by its place in `p.canvases`. Finding: in an inspected paragraph's gap lists, how ranges are grouped follows the number and order of raises, so two data-flow fixes that regrouped 3 and 1 of 67,065 rows were taken back (rebuild/DESIGN.md §5; the comment beside `addGap` in `engines/blink/gaps.ts`). Unmerged alternative, not part of X2: branch `ra-x2-blink-alt-positions`. No other shared file changed.
- webkit owner, 2026-09-19 (re-architecture step 2, X2): no ported rule edited; tier 1 repeats only on webkit-host's references (58,144 cases without facts, 56,498 with) and the same on the other four; tier 2 in webkit-host, both orders and both configurations, 0 transitions. The WebKit port holds its Canvas contexts (four per box, an inspected paragraph's box facts theirs, the list in `WebKitPrepared.contexts`, which replaces `measurer`) and no longer imports the index API, the memo or the log; no function takes a measurer. New field `WebKitBox.spaceWidth`, null where the box's white space is deferred; `hyphenWidth` and `hyphenGlyphsDiffer` are gone. Its tests' list of asked questions reads `font|letter spacing|text` and holds repeats. For the tests owner: rebuild/tests/coverage-map/webkit.txt is from before X1 and names old line numbers. No shared file changed.
- gecko owner, 2026-09-19 (re-architecture step 2, X2): no ported rule edited; tier 1 repeats only on Firefox's references (52,444 cases without facts, 52,498 with) and the same on the other four; tier 2 both orders 0 transitions in Firefox, both configurations. The Gecko port holds its Canvas contexts (`GeckoPrepared.contexts`, `GeckoTextRun.context`) and no longer imports the index API, the memo or the log; `GeckoPrepared.measurer` is gone; its six module-level memos are `GeckoPrepared.inWord` and `GeckoUnit.groups`. Findings outside the step, left as they were: a boundary U+00A0 has been measured as U+0020 since a4f23b8, and fixing it changes a Canvas question; five `suite-sample` cases differ in native widths under the plain predictor without being marked history-dependent (rebuild/tests/known-tail.json, `gecko/plain-predictor-fallback-state`). No shared file changed.
- orchestrator, 2026-09-19 (re-architecture step 2's X2 and step 3, merged): the three X2 branches and the painter step are merged; against the references frozen before them tier 1 exits 3 with repeats only in all six (0 predictions changed, 0 dropped only, 0 other or new questions), and the references were recorded again at the X2 merge. Decisions. The plan's tripwire (within 2× step 0's times) tripped once, on Firefox's giants along the inspected path (15.3 s of prediction against 4.2 s, 3.6×; their plain path is 1.28×): accepted, because the tripped path is the inspected one and what would answer it is a store found by string, which decision 4 keeps for after profiling. The font checks' call-local list of asked questions is kept: no engine needs it for correctness, and deleting it only adds Canvas calls (4,692 Chrome and 27,014 webkit-host cases without facts would repeat a font-check question). Blink's finding that gap ranges are grouped by the number and order of raises goes to X3, which gives gap lists a canonical form so the two flows taken back can return; Chrome is frozen again at that merge. The painter's fix for the two open `twins` rows also waits for X3, since it changes recorded painter limits. Two candidates are written down with numbers as the first items for the profiling phase: units of equal text in one prepared paragraph sharing one record of what measuring found, and per-fill positions and safe flags kept on the item's shape result (research/ARCHITECTURE-PLAN-2.md §10). Documents: rebuild/DESIGN.md §2.1, §2.8, §3, §4.2, §4.5, §4.6, the new §4.7 ("What removing the memo cost"), §5 and §8.1; rebuild/TESTS.md and rebuild/lab/README.md (who counts Canvas questions, the plain predictor's runs at X2, the tripwire's readings, the painter differential's plant, check 8's empty list); rebuild/REPORT.md §1.1 and §1.2 (the painter's lines); rebuild/tests/known-tail.json (64 items); dated notes in research/ARCHITECTURE-PLAN-2.md.
- blink owner, 2026-09-19 (re-architecture step 2, X3): the model clean-up edits no ported rule and changes no row: tier 1 the same on all 389,646 cases (exit 3 by Chrome's string storage rule alone, because `engines/blink/shape.ts` changed); tier 2 in Chrome, both orders and both configurations, 0 transitions, and every row equals the references' recording. `BlinkPrepared.styles` holds one `BlinkStyle` record per style (its contexts, `oneByteContexts`, `canvasSplitsWords`, `hanKerning`) in place of five arrays by style, and `textAlign`, `needsAccurateEndPosition`, `sourceLength` and `wordSpacingAnywhere` are gone from the prepared paragraph; items are a tagged union (`TextItem`, `ControlItem`, `TagItem`, `AtomicItem`), and an item's shaping group is `groupOfUnit` at its start; no Map or Set is left in the port; new file rebuild/src/engines/blink/contexts.ts (`styleContexts`, `raw16Of`), and the port's one cycle of function imports is down to shape.ts, limits.ts and gaps.ts. Three more jobs change recorded Chrome rows, each in its own commit. (a) Chrome's gap lists are handed out canonical (`gaps.ts` `canonicalGaps`), and the two flows X2 took back are in: 473 rows without facts and 303 with them change byte for byte, none after canonicalizing both sides (rebuild/DESIGN.md §5; the proof is `.artifacts/tests/runs/ra-x3-blink/tools/canonical-proof.ts`). (b) rebuild/src/paint.ts `PaintRules.lineStartScript.scriptsOf` takes the painted block's direction as a third parameter, passed at its three call sites; no painted form changes, and Blink's `script-at-line-start` moves in 461 Chrome rows of each configuration. (c) An inspected paragraph no longer makes unused one-byte hyphen contexts; the context count falls in 3,745 cases of each configuration. rebuild/tools/gen-blink-data.ts writes HarfBuzz's OpenType language tags as records sorted by code (`generated/break-tables.ts` `blinkOtLanguageTags`); every other generated export is byte-equal. rebuild/tools/citations/accepted-blink.json: three losses accepted by name. Chrome's two replay references are frozen again with this stage. For the shared owner: rebuild/tests/replay.ts `STORAGE_PATHS` should add `rebuild/src/engines/blink/contexts.ts`; a comment in rebuild/src/measure/font-checks.ts and a `source` in the rule registry name `engines/blink/shape.ts styleContexts`, which is `engines/blink/contexts.ts styleContexts` now.
- webkit owner, 2026-09-19 (re-architecture step 2, X3): no ported rule edited; tier 1 exits 0 on all six references, 0 predictions and 0 questions changed; the whole function set against the start commit's, question sequences included, differs in 0 of 63,987 cases in either configuration; tier 2 in webkit-host, both orders and both configurations, 0 transitions. `LineRun` is a tagged union and the breaker's result a union on its action; `trailingWhitespace` and `Line.trimmable` are a record or null; element items carry `sourceOffset`. New files rebuild/src/engines/webkit/items.ts (the item builder, out of content.ts) and history.ts (the history worlds and `pageHistoryGaps`, out of gaps.ts), and no import cycle is left, type imports included. `index.ts` composes `inspectLine` as `lineGaps`, then `pageHistoryGaps`, then `lineGeometry`, so `gaps.ts` `lineGaps` alone no longer includes `page-history`. `paragraphGaps` returns copies. A box's inspection record is made in one step (`gaps.ts` `boxMade`; `collectBoxFacts` and `inspectParagraph` are gone), and `familyNames` and `namedFamily` are in fonts.ts. Gone: `WebKitPrepared.paragraph` and `runTexts`, `WebKitBox.hasStrongDirectionality`, `firstNamedGeneric` and `wordSpacing`, the always-null `hyphenWidth` fields, `expansionShares`, `dictionaryRangeStartsWithMark`, `boxOfRun` in output.ts and `elementOffsetOnLine`. rebuild/tools/citations/accepted-webkit.json: two losses accepted by name. Not built: handing prepare's merged-glyph record to a line's inspection (rebuild/DESIGN.md §4.7). For the shared owner: the comment on `Prepared` in rebuild/src/index.ts says the engines' state holds the paragraph it laid out, and WebKit's no longer does, since nothing read it. For the tests owner, as at X2: rebuild/tests/coverage-map/webkit.txt is from before X1. No shared file changed.
- gecko owner, 2026-09-19 (re-architecture step 2, X3): no ported rule edited; tier 1 the same on all six references, tier 2 both orders 0 transitions in Firefox, both configurations. `GeckoPrepared` holds `leaves: GeckoLeaf[]` instead of `runStarts`, `runStyles`, `runParents`, `runLangs` and `letterSpacingAu`, `tabs` instead of `tabUnit` and `tabSpacingPrefix`, and no `inWord` (`GeckoUnit.inWord` holds the records and the group count; `GeckoUnit.groups` is gone); a text run is cut into shaping units once, where the port of SplitAndInitTextRun sets the glyph flags; `GeckoFrame.is8bit` and `GeckoTextRun.is8bit`, `.pairKerning`, `.scriptLookups` and `.joining` are gone; `frameOfSource` is `holderOfSource`; `fonts.ts` `primaryFamilyOf` and `props.ts` `isEmojiModifier` are gone. Deviation from the plan: `emergencyUnconfirmed` is a list in text order, not flags per offset. For the tests owner: rebuild/tests/coverage-map/gecko.txt names line numbers from before X1. No shared file changed.
- orchestrator, 2026-09-19 (re-architecture step 2's X3, merged): the three X3 branches are merged (f7884ba, 960fd6f, 772628f). Blink's three row-changing jobs were accepted after the orchestrator ran the owner's proof again on the merged tree, in both configurations, 67,065 cases each: 473 rows without facts and 303 with them differ byte for byte from the references; after both sides' gap lists are made canonical by the script's own definition, the painter's limits left out, 67,065 of 67,065 cases are equal and every new list is canonical; with the limits in, the cases that still differ are exactly the 460 of the painter's job, where only `script-at-line-start` moves (on 470 more lines and 31 fewer) and no painted DOM changes; the context count falls in 3,745 cases. On the merged tree tsc is clean for six projects, the tests pass, tier 1 exits 0 for Firefox and webkit-host and 1 for Chrome exactly as accounted (926 and 756 predictions changed: 473 and 303 by gap lists, 461 by painter limits, 8 by both; the rest repeats only or the context count), the function set's plain and pure checks exit 0, and citations lose 0. rebuild/tests/replay.ts `STORAGE_PATHS` adds `rebuild/src/engines/blink/contexts.ts` (c7f3c3d). Chrome's references were recorded again and frozen at the X3 merge. Recorded plainly: X3 did not bring line counts down (non-test lines: Blink 7,118 to 7,195, WebKit 6,132 to 6,074, Gecko 5,848 to 5,850); it removed state and reads that cut across stages, and the ports are mostly ported logic with citations (rebuild/DESIGN.md §3). Not built, with the owners' reasons (research/ARCHITECTURE-PLAN-2.md §8, X3): WebKit's item hand-over, Blink's reshape records that hold their own positions, Gecko's flags per offset. Known tail: `blink/painter-brackets-shaped-otherwise-than-the-paragraph` is closed, and `gecko/plain-predictor-fallback-state` went into `gecko/process-font-fallback-state` (63 items, 594 named cases). Documents: rebuild/DESIGN.md §2.8, §2.9, §3 (each port's data since X3, and the line counts), §4.6, §4.7, §5 (canonical gap lists), §7 and §8.1; rebuild/TESTS.md and rebuild/lab/README.md (X3's gates, the proof script under `.artifacts`, the storage rule's paths, the plain predictor's runs, the times); research/INCREMENTAL-API-READING.md (an appendix: the prepared facts that read across a forced break or over the whole text); dated notes in research/ARCHITECTURE-PLAN-2.md; the rule registry's `source` for `shared/env/canvas-checks`. Left for the final step, because they are in `rebuild/src`: the comment on `Prepared` in rebuild/src/index.ts, the comment in rebuild/src/measure/font-checks.ts that names `engines/blink/shape.ts styleContexts`, the header of rebuild/src/engines/webkit/index.ts, whose import order lists history after content although content.ts imports history.ts, and Gecko's `paragraphGaps`, which returns the prepared list while Blink's and WebKit's return copies since X3 (`src/index.ts` `concat`s it into a fresh list whose entries are still the prepared paragraph's objects).
- shared owner, 2026-09-19 (re-architecture step 4, the last deletions): not additive, and nothing a port uses. `rebuild/src/measure/canvas.ts` loses the index API (`Measurer`, `createMeasurer`, `measureContext`, `measureText`, `measureTextBounds`) with its memo and log, and `rebuild/src/measure/log.ts` is gone: the file is `CanvasSettings`, `Context`, `contextFor`, `width` and `bounds`, and the annotation of `blink/measure/string-reaches-canvas-as-built` sits on `width`. `rebuild/src/model.ts` loses `LineOf` and `LineResultOf`; their comments, with the engines' citations for a line box that holds content, a refused slot and a joined line edge, are on `FillResultOf` and `LinePieces`, and `rebuild/src/index.ts` no longer re-exports the two. `rebuild/src/test-lines.ts` exports `TestLine<Start, Geometry>`, which `engines/gecko/gecko.test.ts` reads where it read `LineOf` (its import and one type alias: the step's one touch under `engines/`). `rebuild/tests/replay.ts` reads reference format 2 alone. The registry retires `shared/measure/fresh-measurer-per-layout` and `shared/measure/memo-per-layout` for `shared/measure/contexts-per-prepared-paragraph` and `shared/measure/asking-again-answers-the-same` (through `rule-changes.json`). Knip: `lab/cases/font.ts` `formatFontShorthand`, `lab/font-facts.ts` `parseFamilyList` and `lab/types.ts` `InlineElement` are gone, and `knip.config.ts` names the tiers' newer tools. `rebuild/tests/seed-facts-20260916.sh` is removed (a one-off that would write over merged facts files). Chrome's lab gate seeds of both configurations hold `twins` (0 lost, 0 left, 0 gained, 380 cases only now). The coverage maps are regenerated. Tier 1 is the same on all 389,646 cases with 0 questions changed (exit 3 for Chrome by the string storage rule alone, since `src/measure` changed); Chrome's tier 2 forward in both configurations shows 0 transitions; the plain and pure checks, the citation ledger (0 lost) and the painter differential are clean. DESIGN.md is rewritten for the library as it is, and REPORT.md, CHARTER.md, TESTS.md and the lab README are brought up to date.
- blink owner, 2026-09-19 (correctness round 5, research/CORRECTNESS-ROUND-5.md): no shared file changed. `engines/blink/shape.ts`: a side of the pair window (`pairAdjust16`, `windowAdjust16`) that holds only default-ignorable characters and marks reaches to the next cluster (`holdsNoBase`, which was `allDefaultIgnorable`), as HarfBuzz's lookups skip such glyphs (hb-ot-layout-gsubgpos.hh:558-571, hb-kern.hh:58). SHY and a kasra between two beh are one cluster of no advance, and a window that stopped at it put the whole pair adjustment on the last letter. New `engines/blink/pair-window.test.ts` and probe `rebuild/probes/blink-cr5.ts` (K pair placement in 26 kerning families, L lam-alef cluster membership in 31 Arabic family names, Z a cluster of an ignorable character and a mark at overflow widths; Chrome only, measurement only). 15 cases of the main-only list pass in both configurations, all accidental passes of main, 0 lost; main's true passes that still fail stay 344 without facts and 78 with them, and the negative result for Chrome's two big groups stands (DESIGN.md §5). Until Chrome's references are recorded again tier 1 exits 1 there: 569 and 416 cases ask a new question, 39 and 56 other questions, 129 and 265 predictions changed in gap lists (without facts 329 `script-context` line entries and 3 paragraph entries fewer, 2 `unsafe-to-break` more) and in 2 cases' cluster advances, 0 line ranges. Tier 2 in both orders and both configurations: 4 transitions per configuration, all on `c-bff5270008f33766`, to a pass. The plain path asks 234.31 questions a paragraph before and after. Not landed: a guard for joined-letter positions that run backwards, traced on all 10 of its cases and fixing none. Two edges stay open: under letter spacing the position before a cluster without a base is off by the spacing, as before, and `c-d1d894359107d0a8` passes for a reason nobody traced. For the registry: `blink/shape/default-ignorables-skipped-in-pair` is restated.
- webkit owner, 2026-09-19 (correctness round 5): no shared file changed. `engines/webkit/measure.ts`: the font code path of a width is the measured string's, as `FontCascade::width` chooses it from the TextRun it is handed (FontCascade.cpp:304-309, :708-730; TextUtil.cpp:84-89), not the box's: `isComplexCodePath` moved there from `content.ts`, `mergedGlyphs` separates a letter-spaced string's merged pairs where the string takes the simple path, and `gaps.ts` reads the same path for `control-character-width` (its own commit, a67121a, which can be reverted alone). `WebKitBox.spaceWidth` is a number, measured once as the box is made (`content.ts` `makeBox`), so a box whose white space is deferred no longer asks its space at every read; `singleSpaceWidth` returns the field. `index.ts`'s header lists history before content, and `checks.ts` names the ligature recipe the port has. 21 cases of the main-only list pass (11 true passes of main), 0 lost of 2,056; 21 other list cases are page history and pass alone (research/MAIN-PASSES-REFRESH.md). The plain path asks 36.51 questions a paragraph without facts and 18.83 with them (39.32 and 21.65), the nine giants 295,170 calls (707,622). Until webkit-host's references are recorded again tier 1 exits 1 there: 154 cases change in their gaps alone, 22,450 and 22,426 ask in another order, 7,020 and 7,044 ask a space the record lacks; a questions-only freeze won't do. Tier 2 in both orders and both configurations: 3 painter rows lose `control-character-width` from their cover, nothing else moves. Not landed: a second prototype that separates Latin pairs inside complex-path strings (what Core Text does around U+200C inside a run is closed source; a probe has it 1.9px off; it stays on `x-mainfacts-webkit`), and the features-off family, a new kind of fact (DESIGN.md §5). Known limit, not new: under `pre-wrap` with TABs the port measures each TAB segment as its own Canvas string, so its path is the segment's where the DOM's is the whole range's; no set case has it. For the registry: `webkit/measure/code-path-per-measured-string` is new and annotated, and `webkit/measure/letter-spacing-merged-glyphs` and `webkit/gap/control-character-width` are restated.
- gecko owner, 2026-09-19 (correctness round 5): no shared file changed. `engines/gecko/advance.ts`: where the `pairKerning` fact isn't given, Canvas tells which glyph of a kerned pair carries the adjustment, from the app-unit rounding of each glyph (`pairKernedShare`, `placedTotals`, `toldBy`), by the pair's own total or by 16 probe pairs asked once per Canvas context of a prepared paragraph (`askedPlacement`), for pairs Canvas shows the probe letters' face draws (`sameFace`); a joined suffix that a fallback font draws is measured behind its own first letter, and the prefix's side stands in (`sidesAdvance`; U+200D at a string's start takes the first font, gfxTextRun.cpp:3609-3613, :3320-3325). `prepare.ts`: a boundary U+00A0 is measured as itself, as the first port did before a4f23b8. `gaps.ts`: `paragraphGaps` hands out copies, as Blink's and WebKit's do. `lines.ts`: a plain paragraph's break scan reads a candidate inside a word without the questions that only place what crosses it, and asks them where a fit test or an edge needs the whole advance (`roughAdvanceBefore`, `advanceSlack`). New in `types.ts`: `InWordEntry.unrefined`, `InWordSides`, `PairPlacement`, `GeckoPrepared.pairPlacements`, and the stand-in reason `joined-prefix`. New probe `rebuild/probes/gecko-mainfacts.ts` (M1 to M5; Firefox only, measurement only). Main's true passes that still fail: 202 to 76 without facts, 95 to 72 with them, 0 lost. The stop rule was hit once and not obeyed: the first build of pair placement cost the chat mix 30.8 questions a message, and the lazy scan brought it back to 0.00 (0.51 a paragraph on the tier corpus). Until Firefox's references are recorded again tier 1 exits 4: 9,344 cases ask a new question without facts, and 4,560 with them plus 1,444 repeats only; 0 predictions changed. Tier 2 in both orders and both configurations: 0 transitions from a pass. Not landed: a single pair telling alone among three placements (a median of 42 questions, fewer styles told), a longer same-face proof (it would recover 4 refused cases and cost questions on every refused cluster), the two-placement test as prototyped (probe M5 has it telling a wrong value with no gap), device-size U+00A0 under an "Apple Color Emoji"-first list (no tier case), and any code for the 72 contextual joined-form cases (Canvas gives totals only). For the registry: six new Gecko rules.
- critic, 2026-09-19 (correctness round 5): nothing tracked was touched by the critic; its one change is merged as its own commit (3d0a5b3) with a test. `engines/gecko/lines.ts` `breakAndMeasureText` keeps what the scan read at its pending offset in a local (`pendingRead`) and hands it to the next `scanAdvance` as its start, 9 lines. Without it the scan read an earlier candidate twice from a record that `inWordAdvance` could make whole in between (`advanceBefore(group.start)` for a ligature group that reaches past the frame's end), so the running width kept the first value and every later fit test was off by what crosses that offset: a constructed paragraph's plain lines differed from its inspected ones at 22 of 901 widths. No set case has the shape, so every gate passed without the fix. New `engines/gecko/lazy-scan.test.ts` holds it on a stand-in Canvas. The critic's other findings: the plain predictor's run in Firefox is not stable in `heldout-suite-sample` part 0 (that browser process has two fallback-font states), so the 74 cases without facts and 87 with them that both-orders runs read as going from history-dependent to pass are not stable passes; Gecko's plain path cost depends on the width; a held-out probe of the landed code has 1,781 of 1,782 told lines equal to the DOM.
- orchestrator, 2026-09-19 (correctness round 5, merged): the three branches are merged (24b7c23 Blink, 4a38396 WebKit, 95133dc Gecko), then the critic's fix to Gecko's lazy scan with its test (3d0a5b3), then research/CORRECTNESS-ROUND-5.md with the outcome, the cost table and the four reports (b181add). Decisions. Two exceptions to DESIGN.md §4.6 are accepted, as the critic asked them to be written down: Gecko writes two more parts of a prepared paragraph after preparation (`InWordEntry.unrefined`, `GeckoPrepared.pairPlacements`), and `PairPlacement.sameFace` and `otherFace` are verdicts found by a cluster's string, per Canvas context of a prepared paragraph, not a store of measured widths. Gecko's lazy plain scan is accepted with a note for the maintainer: it is the most intricate part of the Gecko port, and the simpler form costs about 31 more Canvas questions a chat message (DESIGN.md §4.6; research/PROFILING-START.md, item 8). WebKit's `control-character-width` change, which was beyond the owner's brief, stays, as the same ported rule in the one other place. Dropping 329 `script-context` entries on passing Chrome lines is accepted: the port no longer measures the string that raised them. Not landed, and why: WebKit's second prototype (a heuristic over closed-source Core Text, 1.9px off in a probe) and its features-off family (a new kind of fact, probed through one API only); Blink's joined-letters guard (traced on all 10 cases, fixes none); Gecko's contextual joined forms (Canvas gives totals only); any per-font table. The 74 and 87 Firefox cases stay named under `gecko/process-font-fallback-state`. Registry: 7 rules added and 3 restated through `rule-changes.json` (549 current; `coverage.ts` lists no unknown annotation). Known tail: 67 items, 783 named cases. Documents: rebuild/DESIGN.md §1.2, §2.8, §3, §4.4 (the round's recipes with their cost and unit of asking), §4.5, §4.6 (the two exceptions and the lazy scan), §4.7 (the numbers since the round), §5 (WebKit's two changed conditions, Blink's `script-context` entries, what Canvas can't be asked) and §8.3; rebuild/TESTS.md and rebuild/lab/README.md (tier 1's exits at the merge, the owners' tier 2 runs, the plain predictor's runs, new tests and probes); research/MAIN-PASSES-REFRESH.md ("After correctness round 5"); research/PROFILING-START.md; rebuild/platform-bugs/LEDGER.md entry 6. Left for whoever owns them, because they were outside this step: the owners' texts for rebuild/CHARTER.md ("Facts no check answers": Gecko's exception with its one inference, WebKit's and Blink's sentences; and its count of annotated rules, 21 of 549 now), research/FACTS-FREE.md's `pairKerning` row, the lazy scan's paragraph in specs/gecko-RESULTS.md, which doesn't say yet that the bound holds only once the scan keeps its last read in a local, and in `rebuild/src` the comments on `pairKerning` in `model.ts` and `measure/font-checks.ts`, which still say Canvas isn't asked.
